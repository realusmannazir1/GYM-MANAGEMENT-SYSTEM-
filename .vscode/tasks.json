{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build & Run FitCore",
            "type": "shell",
            "command": "cmake --build build --target FitCore ; .\\build\\FitCore.exe",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": "$gcc"
        }
    ]
}
